VITE_API_BASE_URL=
VITE_AUTH_SESSION_PATH=/api/v1/auth/session
VITE_PLANNING_WORKSPACE_PATH=/api/v1/planning/workspace
# "local": usa /__dev/planning/workspace e /__dev/session/* (só em `pnpm dev`).
# "http": fala com a API real (backend/Supabase) — obrigatório em produção,
# e também útil em desenvolvimento para testar contra o backend de verdade.
VITE_DATA_SOURCE=
